begin
    read(a)...
    if 1 < a then
        begin
            if 2 < a then
                print(a)
            else
                a := 2
        end
    else
        a:= 3...
    print(a)
end

** Here, because the action table was built on an ambiguous/erroneous rule, the first else should be linked to the second if, and the second else to the first if